{
  "name": "display-what",
  "version": "1.0.0",
  "description": "css display property demonstration",
  "main": "index.html",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update-deps": "npm update",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8080 -c-1 ./"
  },
  "keywords": [
    "css"
  ],
  "author": "Martine Dowden",
  "devDependencies": {
    "http-server": "^0.10.0"
  }
}
